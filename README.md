# Monsters API

## To do :

- [x] Installer `mongoose`
- [x] Création du modèle
```javascript
const monsterSchema = new Schema({
  name: {
    type: String,
    required: true,
  },
  level: {
    type: Number,
    required: true,
  },
  description: {
    type: String,
    required: true,
  },
```

- [x] CRUD API - Update
- [x] CRUD API - Delete

